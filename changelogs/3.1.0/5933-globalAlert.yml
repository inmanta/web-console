description: A global alert system has been implemented to work through a provider (AppAlertProvider) and a hook (useAppAlert). We now also only utilize 1 component (AppAlert.tsx) for all alerts now, both inline and toast, which can be reused throughout the whole console. 
issue-nr: 5933
change-type: patch
destination-branches: [master, iso9]

description: Attribute rows of an instance view are now only clickable when they have an associated action available
issue-nr: "6652"
change-type: "patch"
destination-branches: [master, iso9]
sections:
  bugfix: "{{description}}"

description: The create environment form now shows the Repository field before the Branch field, and both fields have a tooltip with an example value.
issue-nr: 7072
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"

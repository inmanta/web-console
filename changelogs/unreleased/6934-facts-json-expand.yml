description: Facts table rows whose value is JSON, XML, or other multi-line content can now be expanded inline to view it formatted in a read-only code editor with copy and download controls.
issue-nr: 6934
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"

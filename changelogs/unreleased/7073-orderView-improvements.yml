description: Add a different color for the acknowledged status in the order progress bar, add a link to the service instance in the order details row, display the instance display value instead of the raw id, and expand the matching row when clicking on a dependency in the order details rows.
issue-nr: 7073
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"
description: The Events page filters have been moved into a side panel drawer, opened from a filter toggle button that shows the number of active filters, and the events table now scrolls within its own region instead of growing the page.
issue-nr: 6776
change-type: patch
destination-branches: [master]
sections:
  minor-improvement: "{{description}}"

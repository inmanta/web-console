description: Text typed into a list field of the service instance form is now added when you leave the field, so it is no longer lost when submitting without pressing Add.
issue-nr: 7365
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"

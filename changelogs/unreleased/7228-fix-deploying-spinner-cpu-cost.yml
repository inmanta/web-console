description: Replaced the PatternFly spinner in continuously-visible status indicators (deploying resources, in-progress compiles, in-progress orders, running dry runs) with a transform-only spinner, cutting its sustained CPU usage from roughly 33% to 4% of a core per spinner.
issue-nr: 7228
change-type: patch
destination-branches: [master, iso9]
sections:
  bugfix: "{{description}}"

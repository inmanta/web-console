description: When only one option available in select input, then this one is preselected instead of default placeholder prompt
sections: 
  minor-improvement: "{{description}}"
issue-nr: 4127
change-type: patch
destination-branches:
  - master
  - iso5

description: A field's suggested values can depend on another field through ${form.<path>}/${self.<path>} references; the dependent field re-queries its suggestions when the source changes. Dependency cycles and missing references surface as model errors.
issue-nr: 7011
change-type: minor
destination-branches: [master, iso9]
sections:
  feature: "{{description}}"

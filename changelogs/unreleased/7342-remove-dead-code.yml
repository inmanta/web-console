description: |
  Removed dead code and unused dependencies across the console: orphaned components and barrel
  files, unused exports, types and enums, stale test mocks and fixtures, and a number of build and
  lint dependencies that were no longer referenced.
issue-nr: 7342
change-type: patch
destination-branches: [master]
sections:
  other-note: "{{description}}"

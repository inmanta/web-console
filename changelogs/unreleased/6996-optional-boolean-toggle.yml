description: Optional boolean attributes in the service instance form are now shown as a True/False toggle group (with no selection representing the unset value) instead of radio buttons.
issue-nr: 6996
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"

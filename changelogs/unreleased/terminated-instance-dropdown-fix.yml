description: "Fix issue with misleading visual state of Diagnose button and misplaced 'back' button for terminated instances"
change-type: patch
destination-branches: [master, iso7]
sections:
  bugfix: "{{description}}"

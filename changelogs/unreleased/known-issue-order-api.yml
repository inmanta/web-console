---
description: "The order API cannot unlink an inter-service relation and delete the target instance of that relation in the same order. A workaround would be to perform this operation using the normal API to modify service instances."
change-type: patch
destination-branches: [master, iso8, iso7]
sections:
  bugfix: "{{description}}"

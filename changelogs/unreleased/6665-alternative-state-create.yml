description: It is now possible to create an instance in an alternative state when those are defined in the service model.
issue-nr: "6665"
change-type: patch
destination-branches: [master, iso9]
sections:
  minor-improvement: "{{description}}"

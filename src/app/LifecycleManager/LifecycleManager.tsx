import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const LifecycleManager: React.FunctionComponent<any> = props => {
  return (
    <PageSection>
      <Title size="lg">Lifecycle Manager Page Title</Title>
    </PageSection>
  );
};

export { LifecycleManager };
